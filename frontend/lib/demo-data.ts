import type { Story } from "@/types";

/**
 * UPDATES Real-Time Social Media Analytics Platform.
 * Per project requirements, all hardcoded demo datasets have been purged.
 * Real data is ingested and streamed dynamically from backend APIs.
 */
export const stories: Story[] = [];
